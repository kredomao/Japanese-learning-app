# 日本語学習アプリ - アーキテクチャ設計書

## 📋 目次

1. [概要](#概要)
2. [ディレクトリ構成](#ディレクトリ構成)
3. [データ構造設計](#データ構造設計)
4. [コアロジック設計](#コアロジック設計)
5. [状態管理設計](#状態管理設計)
6. [拡張性の考慮](#拡張性の考慮)

---

## 概要

### 目的
日本語学習アプリ（ことわざ・言い回し）のバックエンド・ロジック実装

### 設計原則
- **UI非依存**: ロジックはUIに依存しない純粋関数
- **シンプル**: MVP（最小構成）から始める
- **拡張性**: 将来的な機能追加に対応できる設計
- **型安全**: TypeScriptで型安全性を確保

### 技術スタック
- **言語**: TypeScript
- **フレームワーク**: Next.js (App Router)
- **状態管理**: LocalStorage（MVP）、将来的にバックエンド移行可能
- **データ形式**: JSON

---

## ディレクトリ構成

```
src/
├── types/                    # 型定義（ドメインモデル）
│   ├── index.ts             # 基本型（Phrase, UserState, etc.）
│   ├── vocabulary.ts        # ボキャブラリー関連型
│   └── ai.ts                # AI関連型
│
├── data/                     # 静的データ（JSON）
│   ├── phrases.json         # ことわざ・言い回しデータ
│   ├── vocabulary.ts        # ボキャブラリーデータ
│   └── achievements.ts      # 実績・報酬定義
│
├── lib/                      # ビジネスロジック（純粋関数）
│   ├── experience.ts        # 経験値・レベル管理
│   ├── learning.ts          # 学習完了処理
│   ├── storage.ts           # LocalStorage管理
│   ├── gamification.ts     # ゲーミフィケーション
│   ├── quiz.ts              # クイズ機能
│   ├── ai-service.ts        # AI API連携
│   └── index.ts             # エクスポート集約
│
├── hooks/                    # React Hooks（UI層）
│   ├── useUserState.ts      # ユーザー状態管理
│   ├── useVocabulary.ts     # ボキャブラリー管理
│   └── useAI.ts             # AI機能
│
├── app/                      # Next.js App Router
│   ├── api/                 # API Routes
│   │   └── ai/              # AI API
│   ├── layout.tsx           # レイアウト
│   └── page.tsx             # メインページ
│
└── index.ts                 # メインエクスポート
```

### 設計思想

```
┌─────────────────────────────────────────┐
│           UI Layer (Bolt.new)           │
│  - React Components                     │
│  - User Interactions                    │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│        Hooks Layer (React)               │
│  - useUserState, usePhraselearning      │
│  - UI状態とロジックの橋渡し              │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│      Business Logic Layer (lib/)         │
│  - 純粋関数（UI非依存）                  │
│  - 経験値、学習、ストレージ              │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│        Data Layer                        │
│  - JSON (phrases.json)                  │
│  - LocalStorage                          │
└─────────────────────────────────────────┘
```

---

## データ構造設計

### 1. ことわざ・言い回しデータ（Phrase）

```typescript
interface Phrase {
  id: number;              // 一意のID
  phrase: string;          // ことわざ・言い回し本文
  reading: string;         // 読み仮名
  meaning: string;         // 意味・解説
  example: string;         // 例文
  level: number;           // 難易度レベル (1-5)
  tags: string[];         // タグ（カテゴリ分類用）
}
```

**保存場所**: `src/data/phrases.json`

**設計意図**:
- `id`は数値で一意性を保証
- `level`で難易度管理（将来的にレベル制限機能に使用）
- `tags`でカテゴリ別学習・統計に使用

### 2. ユーザー状態（UserState）

```typescript
interface UserState {
  // 基本ステータス
  level: number;                    // 現在のレベル
  experience: number;              // 現在の経験値
  totalExp: number;                // 累計経験値
  learnedPhraseIds: number[];      // 学習済みフレーズID配列
  
  // 連続学習
  streak: number;                  // 連続学習日数
  bestStreak: number;              // 最高連続記録
  lastLearnedAt: string | null;   // 最終学習日時（ISO形式）
  
  // ゲーミフィケーション（オプション）
  unlockedAchievements: UnlockedAchievement[];
  currentTitle: string;
  unlockedTitles: string[];
  dailyMissions: DailyMissionState | null;
  
  // 統計
  stats: UserStats;
}
```

**保存場所**: LocalStorage（`storage.ts`で管理）

**設計意図**:
- `learnedPhraseIds`は配列で管理（重複チェックが容易）
- `lastLearnedAt`で連続学習判定
- ゲーミフィケーション要素はオプション（MVPでは最小限）

### 3. 学習結果（LearningResult）

```typescript
interface LearningResult {
  success: boolean;                // 保存成功フラグ
  learnedPhraseId: number;        // 学習したフレーズID
  experienceGained: number;       // 獲得経験値
  leveledUp: boolean;             // レベルアップフラグ
  newLevel: number;               // 新しいレベル
  newExperience: number;          // 新しい経験値
  userState: UserState;          // 更新後のユーザー状態
}
```

**設計意図**:
- 処理結果をオブジェクトで返す（UIで通知表示に使用）
- `leveledUp`フラグでレベルアップ通知を実装可能

---

## コアロジック設計

### 1. 経験値・レベル管理（`lib/experience.ts`）

#### 関数一覧

```typescript
// 経験値を獲得（学習1件完了で +10）
gainExperience(currentState: UserState): ExperienceResult

// レベルアップ可能かチェック
checkLevelUp(state: UserState): boolean

// 次のレベルに必要な経験値を計算
getRequiredExpForLevel(level: number): number

// 経験値進捗を取得
getExpProgress(state: UserState): {
  current: number;
  required: number;
  percentage: number;
}
```

#### 仕様

- **学習1件完了**: experience +10
- **レベルアップ必要EXP**: level × 100
- **レベルアップ判定**: 自動的に複数回レベルアップに対応

#### 使用例

```typescript
import { gainExperience, checkLevelUp } from './lib/experience';

// 経験値を獲得
const result = gainExperience(currentState);
console.log(result.leveledUp); // true/false
console.log(result.newLevel);   // 新しいレベル

// レベルアップ可能かチェック
const canLevelUp = checkLevelUp(state);
```

### 2. 学習完了処理（`lib/learning.ts`）

#### 関数一覧

```typescript
// 「覚えた」ボタン押下時の処理（MVP版）
markAsLearned(phraseId: number): LearningResult

// 学習完了処理（フル機能版・ゲーミフィケーション対応）
completeLearning(
  currentState: UserState,
  phraseId: number,
  phrase?: Phrase,
  allPhrases?: Phrase[]
): LearningResult
```

#### 処理フロー

```
1. 学習済みIDを保存（重複チェック）
   ↓
2. 経験値を加算（+10）
   ↓
3. レベルアップ判定
   ↓
4. ユーザー状態を更新
   ↓
5. LocalStorageに保存
   ↓
6. 処理結果を返す
```

#### 使用例

```typescript
import { markAsLearned } from './lib/learning';

// 「覚えた」ボタンを押したとき
const result = markAsLearned(phraseId);

if (result.success) {
  console.log(`獲得経験値: ${result.experienceGained}`);
  console.log(`レベルアップ: ${result.leveledUp}`);
  // UIで通知表示など
}
```

### 3. 状態管理（`lib/storage.ts`）

#### 関数一覧

```typescript
// ユーザー状態を読み込み
loadUserState(): UserState

// ユーザー状態を保存
saveUserState(state: UserState): boolean

// ユーザー状態をリセット
resetUserState(): boolean

// 連続学習日数の更新判定
checkStreakUpdate(lastLearnedAt: string | null): {
  shouldIncrement: boolean;
  shouldReset: boolean;
}
```

#### 設計意図

- LocalStorageを抽象化（将来的にバックエンドAPIに置き換え可能）
- エラーハンドリングを内包
- 初期値のマージ機能で後方互換性を確保

---

## 状態管理設計

### MVP（最小構成）

```
┌─────────────────┐
│  LocalStorage   │ ← 永続化
└────────┬────────┘
         │
┌────────▼────────┐
│  loadUserState  │ ← 読み込み
│  saveUserState  │ ← 保存
└────────┬────────┘
         │
┌────────▼────────┐
│  Business Logic │ ← 純粋関数
│  (lib/)         │
└────────┬────────┘
         │
┌────────▼────────┐
│  React Hooks    │ ← UI層
│  (hooks/)       │
└─────────────────┘
```

### 拡張時（バックエンド移行）

```typescript
// storage.ts を差し替え
export async function loadUserState(): Promise<UserState> {
  const response = await fetch('/api/user/state');
  return response.json();
}

export async function saveUserState(state: UserState): Promise<boolean> {
  const response = await fetch('/api/user/state', {
    method: 'POST',
    body: JSON.stringify(state),
  });
  return response.ok;
}
```

**設計意図**: インターフェースを統一することで、実装を差し替え可能

---

## 拡張性の考慮

### 1. データソースの拡張

現在: JSONファイル
将来: データベース、API

```typescript
// 抽象化レイヤーを追加可能
interface PhraseRepository {
  getAll(): Promise<Phrase[]>;
  getById(id: number): Promise<Phrase | null>;
  getByLevel(level: number): Promise<Phrase[]>;
}
```

### 2. ストレージの拡張

現在: LocalStorage
将来: IndexedDB、バックエンドAPI

```typescript
// ストレージインターフェース
interface StorageAdapter {
  load(): Promise<UserState>;
  save(state: UserState): Promise<boolean>;
  reset(): Promise<boolean>;
}
```

### 3. 機能の拡張

- ✅ 学習完了処理（実装済み）
- ✅ 経験値・レベル管理（実装済み）
- ✅ ゲーミフィケーション（実装済み）
- ⬜ クイズ機能（準備済み）
- ⬜ 復習機能
- ⬜ 統計・分析機能

### 4. パフォーマンス最適化

- データの遅延読み込み
- キャッシュ戦略
- バッチ処理

---

## MVP実装チェックリスト

### 必須機能

- [x] ことわざ・言い回しのデータ構造
- [x] 学習済み状態管理（LocalStorage）
- [x] 経験値・レベルアップロジック
- [x] 学習完了処理の関数
- [x] 型定義（TypeScript）

### オプション機能（実装済み）

- [x] ゲーミフィケーション（実績、連続学習）
- [x] デイリーミッション
- [x] AI例文生成

---

## まとめ

### 設計の特徴

1. **レイヤー分離**: UI層とロジック層を明確に分離
2. **純粋関数**: テスト容易性と再利用性を確保
3. **型安全**: TypeScriptでコンパイル時エラー検出
4. **拡張性**: インターフェース設計で将来の変更に対応

### 次のステップ

1. UI実装（Bolt.new）
2. テスト追加（Jest/Vitest）
3. パフォーマンス最適化
4. バックエンド移行（必要に応じて）

